spring:
  # nacos
  cloud:
    nacos:
      # 注册中心的配置
      discovery:
        server-addr: http://localhost:8848
        # namespace: mkt_dev
        # group: DEFAULT_GROUP
        register-enabled: false
